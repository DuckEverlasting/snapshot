class Node {
  constructor(data, next=null) {
    this.data = data;
    this.next = next;
  }
}

export default class LinkedList {
  constructor(initNode=null) {
    this.head = initNode;
    this.tail = initNode;
  }

  push(data) {

  }

  pop() {

  }
}